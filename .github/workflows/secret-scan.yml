name: secret-scan-placeholder

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Explain next step
        run: |
          echo "Añade aquí un escaneo real de secretos (por ejemplo gitleaks) cuando el repo se publique." 
